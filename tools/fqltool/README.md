## Status

The Java `fqltool` sources were removed from this branch on 2026-05-24 as part
of the Cassandra Java→Rust rewrite.

Use the Rust tooling in `rust/crates/cassandra-tools` instead.

Historical Java implementation remains available in Java oracle history/branch.
